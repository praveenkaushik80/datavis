# Cube for hosts without a bind mount (Render, Railway, Fly.io, ...). Set DATAFUSION_API_URL,
# DATAFUSION_INTERNAL_TOKEN and CUBEJS_API_SECRET in the host's environment.
FROM cubejs/cube:v1.7.45
COPY cube.js /cube/conf/cube.js
